pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "This is First Jenkins Pipelineee"'
                sh '''
                    echo "This is step script"
                    ls -ltr
                '''
            }
        }
    }
}
